describe('IptReportTabularComponent', () => {
    it('should have a defined component', () => {
        pending();
    });

    it('should display tabular data correctly', () => {
        pending();
    });

    it('should display error message when unable to load data', () => {
        pending();
    });

    it('should navigate to patient list when and indicator is clicked', () => {
        pending();
    });
});
