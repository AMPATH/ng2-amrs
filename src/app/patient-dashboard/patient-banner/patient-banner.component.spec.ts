/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PatientInfoBannerComponent } from './patient-info-banner.component';

describe('Component: PatientInfoBanner', () => {
  it('should create an instance', () => {
    let component = new PatientInfoBannerComponent();
    expect(component).toBeTruthy();
  });
});
